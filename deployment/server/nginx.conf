# NOTE: This Nginx configuration is deprecated in this deployment.
# Caddy is used as the edge proxy and static file server now (see ./server/Caddyfile).
# Kept here only for reference.

server {
    listen 80;
    server_name _;

    location / {
        return 410 "Nginx replaced by Caddy. See deployment/server/Caddyfile\n";
    }
}
